language: python
env:
  - TOXENV=py26
  - TOXENV=py27
  - TOXENV=py32
  - TOXENV=py33
  - TOXENV=py34
  - TOXENV=pypy
  - TOXENV=pypy3
# command to install dependencies
install:
  - pip install -U tox
# command to run tests
script:
  - tox -e ci

notifications:
  email:
    on_success: change
    on_failure: change